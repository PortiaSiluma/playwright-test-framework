{
  "name": "playwright-test-framework",
  "version": "1.0.0",
  "description": "UI and API automation using Playwright (SauceDemo and Restful Booker)",
  "scripts": {
    "test": "npx playwright test",
    "ui": "npx playwright test tests/ui",
    "api": "npx playwright test tests/api",
    "report": "npx playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "monocart-reporter": "^1.3.0"
  }
}
